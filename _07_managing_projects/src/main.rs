// Nesting paths
use std::{cmp::Ordering, io};
// nesting paths using self
// use std::io::{self, Write};

// using global operator * to import all
// use std::collections::*;

fn main() {
    println!("Hello, world!");
}
